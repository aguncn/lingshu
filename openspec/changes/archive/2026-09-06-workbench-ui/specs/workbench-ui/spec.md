## Purpose

把已交付的 P2（空间/任务/文件）与 P3（模型供应商/任务绑模型/提示词库）REST 能力收拢为产品唯一入口：三栏工业风工作台（左栏导航与任务列表、右栏会话与可折叠细节栏），以命令面板、抽屉、暗色模式、状态徽标与时间线构成生产级操作台外壳，并为 P8 会话与 P4~P6 能力预留稳定挂载面。纯前端能力，只消费既有 `/api/*`。

## ADDED Requirements

### Requirement: 三栏工作台外壳与默认路由

系统 SHALL 以三栏工业风工作台作为主界面并作为默认路由：左栏全高包含「新建任务」按钮、主导航（专家·技能·MCP / 自动化 / 资料库）、任务列表区、底部个人信息与设置入口；右栏自上而下为顶部常用任务/提示词模板条、中部会话区、底部可折叠细节栏与输入框。冷启动进入应用 SHALL 直接呈现工作台，无需额外跳转。

#### Scenario: 冷启动直达工作台

- **WHEN** 用户在浏览器打开前端根路径且后端健康检查可用
- **THEN** 直接显示三栏工作台，左栏可见「新建任务」、主导航、任务列表区与个人信息，右栏可见顶部模板条、会话区与底部输入框

#### Scenario: 主导航各区块可达且未实现区块不报错

- **WHEN** 用户依次点击左栏导航「专家·技能·MCP」「自动化」「资料库」
- **THEN** 对应区块以抽屉/面板呈现，后端未交付的能力区块显示稳定空态与说明文案，而非整页报错或空白崩溃

### Requirement: 空间与任务浏览、新建与搜索

系统 SHALL 通过 `GET /api/spaces` 拉取空间列表，并逐空间调用 `GET /api/spaces/<sid>/tasks` 拉取任务，在左栏按空间分组展示任务；SHALL 支持在搜索框按任务标题或空间名即时过滤分组视图；SHALL 支持新建空间（`POST /api/spaces`）与新建任务（`POST /api/spaces/<sid>/tasks`，`task_type` 取受控枚举 `fault/change/alert/general` 之一）；SHALL 支持选中某任务后将其载入右栏会话上下文（会话区、文件树、细节栏随之切换）。

#### Scenario: 任务按空间分组展示且可搜索过滤

- **WHEN** 后端存在多个空间且各含任务，用户在工作台左栏查看任务列表，并在搜索框键入某任务标题/空间名的子串
- **THEN** 任务按空间分组展示；键入后列表即时过滤到匹配项，清空后恢复全量

#### Scenario: 新建任务出现在对应分组并可选中

- **WHEN** 用户经「新建任务」对话框选择某空间、填写标题与 `task_type` 并提交成功
- **THEN** 新任务立即出现在该空间分组下，可被选中并载入右栏，未发生整页刷新

### Requirement: 状态徽标与任务时间线

系统 SHALL 以 Element Plus `ElTag` 徽标呈现任务 `status` 与空间 `visibility` 等枚举状态，状态值不同时徽标样式可区分；SHALL 为选中任务提供事件时间线视图（`ElTimeline`），时间线在无事件数据（P8 会话/审计接入前）时 SHALL 显示占位文案而非报错。

#### Scenario: 状态徽标随枚举值区分展示

- **WHEN** 左栏任务/空间列表含不同 `status`、`visibility` 取值的条目
- **THEN** 每个条目均渲染 `ElTag` 徽标，取值不同其文案与类型样式不同

#### Scenario: 空时间线为占位态

- **WHEN** 选中一个尚无任何事件记录的任务并查看其时间线
- **THEN** 时间线区显示"暂无事件"类占位文案，页面不报错

### Requirement: 顶部常用任务分类与提示词模板快捷入口

系统 SHALL 在右栏顶部调用 `GET /api/prompts?category=task-preset` 展示常用任务类型（写文档/写代码/数据分析/排障等）提示词模板快捷入口；SHALL 支持点击某一模板将其选中并带入当前任务上下文（P8 前体现为会话输入区的选中提示），加载失败时 SHALL 降级为不阻塞的空态。

#### Scenario: 常用任务模板来自预设种子

- **WHEN** 工作台加载且后端已完成迁移（含 `task-preset` 预设）
- **THEN** 顶部快捷入口展示来自该接口的四类模板（写文档/写代码/数据分析/排障），各显示名称与可点选态

#### Scenario: 接口异常时顶部优雅降级

- **WHEN** 提示词接口暂不可用或返回空
- **THEN** 顶部区域显示轻量空态/重试提示，不影响左栏任务浏览与会话区使用

### Requirement: 会话区与任务文件树

系统 SHALL 在右栏中部提供会话区：P8 接入前展示为空态占位（含"智能体会话待接入"说明）；SHALL 为选中任务展示文件树，通过 `GET /api/tasks/<id>/files` 拉取该任务工作空间文件（文件名/大小/mime），无文件时显示空态；点击文件条目 SHALL 依据其类型呈现预览（文本/图片类）或展示该文件元信息（mime/大小/路径），P8 前不要求真实内容解析。

#### Scenario: 选中任务展示其文件树

- **WHEN** 选中某任务且该任务目录下已存在文件（经 P2 文件上传产生）
- **THEN** 文件树列出这些文件（含名称、大小、类型），点击文本/图片类条目可展开预览或元信息面板

#### Scenario: 空任务会话与文件均为占位

- **WHEN** 选中一个没有任何消息记录与文件的任务
- **THEN** 会话区显示"无会话"类占位、文件树显示"无文件"类空态，均不报错

### Requirement: 可折叠细节栏与任务模型绑定

系统 SHALL 在右栏底部提供默认收起的可折叠细节栏，可展开/收起；展开后 SHALL 分区呈现「模型 / 技能 / 资料 / MCP / 工作空间 / 权限模板」：模型分区实连 `GET /api/model-providers` 与 `GET/POST/PATCH /api/tasks/<task_id>/model-config`，支持查看供应商（掩码密钥展示）与为当前任务绑定模型/调参（`temperature`、`max_tokens`、`timeout`）；工作空间分区显示任务所属空间信息；技能/资料/MCP/权限模板分区在对应后端（P4~P6）未交付时 SHALL 为稳定空态占位。

#### Scenario: 细节栏默认收起且可展开收起

- **WHEN** 用户进入任一任务视图，点击细节栏的展开/收起控件
- **THEN** 细节栏初始为收起态，点击后可展开显示六个分区，再次点击恢复收起，状态不与其他任务串扰

#### Scenario: 模型分区绑定与调参经真实接口生效

- **WHEN** 用户在模型分区选择一个已存在的供应商（来自 `/api/model-providers`）为当前任务绑定，并调整 `temperature`/`max_tokens`/`timeout`
- **THEN** 绑定与调参写入该任务的 `model-config`（POST 首次 201 / PATCH 调参 200），细节栏回显掩码密钥与最新参数；重新进入后仍一致

#### Scenario: 未交付能力分区为空态占位

- **WHEN** 展开细节栏查看「技能 / 资料 / MCP / 权限模板」分区
- **THEN** 各分区显示稳定的空态占位与说明（待 P4~P6 后端），交互不报错

### Requirement: 会话输入框与占位会话

系统 SHALL 在会话区底部提供常驻输入框，可输入文本并触发"发送"；在 P8 SSE 会话接入前，发送 SHALL 不请求任何后端会话接口，仅在会话区生成本地占位消息并提示"智能体会话将在后续接入"；切换选中任务或清空操作 SHALL 重置该占位会话区，不残留跨任务内容。

#### Scenario: 发送产生本地占位消息

- **WHEN** 用户在输入框输入一段文本并点击发送（P8 未接入）
- **THEN** 会话区新增一条本地占位气泡并给出"待接入"提示，不发起失败的网络请求

#### Scenario: 切换任务重置会话上下文

- **WHEN** 用户在 A 任务输入过占位消息后切换到 B 任务再切回 A
- **THEN** 会话区不残留 B 任务的输入内容，视图状态随当前选中任务正确切换

### Requirement: 命令面板

系统 SHALL 提供全局命令面板：按 `Ctrl/Cmd+K` 打开，按 `Esc` 或失焦关闭；面板 SHALL 支持模糊过滤命令项（至少含：新建空间、新建任务、切换暗色模式、打开设置、打开注册中心、刷新任务列表），回车执行选中项、方向键切换高亮；执行动作前 SHALL 完成并反馈（含触发新建对话框、切换主题等）。

#### Scenario: 快捷键打开与模糊执行

- **WHEN** 用户在应用任意位置按 `Ctrl/Cmd+K`，输入"暗色"并在结果中选择"切换暗色模式"后回车
- **THEN** 命令面板打开、过滤出匹配命令、回车后执行主题切换并关闭面板

#### Scenario: Esc 关闭且焦点还原

- **WHEN** 命令面板打开后用户按 `Esc`
- **THEN** 面板关闭，页面焦点回到触发前状态，无残留遮罩

### Requirement: 设置中心与注册中心抽屉、暗色模式

系统 SHALL 用 `ElDrawer` 承载设置中心与注册中心，可从左栏底部个人信息/设置入口、主导航或命令面板打开：设置中心含暗色模式开关、后端探活状态（调 `GET /api/health`）；注册中心含「技能 / MCP / 知识库 / 专家」四类入口（对应后端 P4~P6 交付前为空态占位）；暗色模式 SHALL 通过 Element Plus `dark` 主题即时生效，并持久化到 `localStorage`，刷新页面后保持用户选择。

#### Scenario: 设置中心切换暗色并持久化

- **WHEN** 用户在设置中心（或命令面板）切换暗色模式，随后刷新页面
- **THEN** 界面即时切换为暗色，刷新后仍为暗色；再次切换可回到亮色并同样保持

#### Scenario: 注册中心抽屉空态可浏览

- **WHEN** 用户打开注册中心抽屉并浏览「技能 / MCP / 知识库 / 专家」各入口
- **THEN** 抽屉正常承载各入口视图，未交付的后端区块显示空态说明，可关闭不报错
